{{define "server-error"}}
    {{template "base" .}}
{{end}}

{{define "title"}}Server Error{{end}}

{{define "content"}}


<!DOCTYPE html>
<html lang="en">
<head>
  <title>Server Error</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/static/css/tailwind-styles.css">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="bg-white rounded-lg shadow-lg p-8 max-w-md text-center">
    <div class="flex justify-center mb-4">
      <img src="/static/img/error.svg" alt="Error icon" class="w-16 h-16 text-red-500" />
    </div>
    <h1 class="text-xl font-bold text-red-600 mb-2">500</h1>
    <h2 class="text-2xl font-semibold mb-2">Internal Server Error</h2>
    <span class="text-gray-600 mb-6">Sorry, something went wrong on our end.</span>
    <br>
    <span class="text-gray-600 mb-6">Please try again later.</span>
  </div>
</body>
</html>

{{end}}